   Test file: pythonslisp/testing/test11-controlStructs.lisp... 
       1> (setf val1 3)

         PASSED!

       2> (cond
   ((= val1 1)
                "one")
   ((= val1 2)
                "two")
   ((= val1 3)
                "three"))

         PASSED!

       3> (case val1
   (1
       "one")
   (2
       "two")
   (3
       "three")
   )

         PASSED!

       4> (defun countdown1 (num)
   (while (> num 0)
      (let ()
         (writeLn! num)
         (setf num (- num 1))
         )
      )
   )

         PASSED!

       5> (countdown1 5)

         PASSED!

       6> (defun countdown2 (num)
   (while (> num 0)
      (let ()
         (writeLn! num)
         (setf num (- num 1))
         )
   )
)

         PASSED!

       7> (countdown2 3)

         PASSED!

       8> (dolist (item '(0 1 2 3 4 5))
   (writeLn! item))
; --- dolist with result form ---
; result form evaluated after loop; var = NIL

         PASSED!

       9> (let ((total 0)) (dolist (x '(1 2 3) total) (setf total (+ total x))))
; result form can be any expr (evaluated with var = NIL)

         PASSED!

      10> (dolist (x '(a b c) 'done) (write! x))

         PASSED!

      11> (dotimes (i 3) (writeLn! i))

         PASSED!

      12> (let ((sum 0)) (dotimes (i 5) (setf sum (+ sum i))) sum)

         PASSED!

      13> (progn 1 2 3)

         PASSED!

      14> (let ((n 5)) (progn (setf n (* n n)) n))

         PASSED!

      15> (let* ((a 2) (b (* a 3))) b)

         PASSED!

      16> (let* ((x 10) (y (+ x 5))) y)

         PASSED!

      17> (eval '(+ 1 2))

         PASSED!

      18> (eval '(list 1 2 3))

         PASSED!

      19> (parse "(+ 1 2)")

         PASSED!

      20> (parse "42")

         PASSED!

      21> ;;; Error: cond requires at least one clause
(cond)

         PASSED!

      22> ;;; Error: let requires at least 2 arguments
(let)

         PASSED!

      23> (block myblock 1 2 3)
; block with no body returns NIL

         PASSED!

      24> (block myblock)
; return-from exits the block immediately, skipping remaining forms

         PASSED!

      25> (block myblock
   (return-from myblock 42)
   99)
; return-from with no value returns NIL

         PASSED!

      26> (block myblock
   (return-from myblock)
   99)
; return-from from inside a nested expression

         PASSED!

      27> (+ 1 (block escape
        (+ 10 (return-from escape 5))))
; nested blocks with same name innermost is exited

         PASSED!

      28> (block foo
   (block foo
      (return-from foo 1))
   2)
; return-from to outer block bypasses the inner block entirely

         PASSED!

      29> (block outer
   (block inner
      (return-from outer 99))
   42)
; block used as early-exit in a search loop

         PASSED!

      30> (defun find-even (lst)
   (block found
      (dolist (x lst)
         (if (evenp x) (return-from found x)))
      nil))

         PASSED!

      31> (find-even '(1 3 5 4 7))

         PASSED!

      32> (find-even '(1 3 5 7))
; --- block nil and return ---
; (block nil ...) acts like a named block

         PASSED!

      33> (block nil 1 2 3)
; (return value) exits (block nil ...)

         PASSED!

      34> (block nil
   (return 42)
   99)
; (return) with no value returns NIL

         PASSED!

      35> (block nil
   (return)
   99)
; dotimes with early (return ...)

         PASSED!

      36> (let ((sum 0))
   (dotimes (i 10)
      (if (= i 5) (return sum))
      (setf sum (+ sum i)))
   sum)
; dotimes with result form

         PASSED!

      37> (let ((total 0)) (dotimes (i 5 total) (setf total (+ total i))))
; dotimes result form - var = count when evaluated

         PASSED!

      38> (dotimes (i 5 i) nil)
; dolist with early (return ...)

         PASSED!

      39> (dolist (x '(1 2 3 4 5))
   (if (= x 3) (return x)))
; Error: stale return-from (no matching block active)

         PASSED!

      40> (return-from nowhere 42)

         PASSED!

      41> (return 42)

         PASSED!

      42> (block 42 1 2 3)

         PASSED!

      43> (return-from 42 99)

         PASSED!

      44> (let ((acc 0)) (for (i 0) (< i 5) (+ i 1) (setf acc (+ acc i))) acc)
; nextForm references var: double each step (1+2+4+8+16 = 31)

         PASSED!

      45> (let ((acc 0)) (for (i 1) (<= i 16) (* i 2) (setf acc (+ acc i))) acc)
; never-run loop (condition false from start) returns NIL

         PASSED!

      46> (for (i 0) (< i 0) (+ i 1) (setf i 99))
; collect into a list

         PASSED!

      47> (let ((lst nil))
   (for (i 0) (< i 4) (+ i 1) (setf lst (cons i lst)))
   (reverse lst))
; early exit with (return ...)

         PASSED!

      48> (for (i 0) (< i 10) (+ i 1)
   (if (= i 3) (return i)))
; loop variable is local — does not leak into outer scope

         PASSED!

      49> (let ((i 99)) (for (i 0) (< i 3) (+ i 1) nil) i)
; string walking via index

         PASSED!

      50> (let ((s "hello") (acc ""))
   (for (i 0) (< i (length s)) (+ i 1)
      (setf acc (ustring acc (char s i))))
   acc)
; Error: initSpec not a list

         PASSED!

      51> (for x (< x 5) (+ x 1) nil)

         PASSED!

      52> (for (x) (< x 5) (+ x 1) nil)

         PASSED!

      53> (for (42 0) (< 42 5) (+ 42 1) nil)

         PASSED!

      54> (for (i 0) (< i 5) (+ i 1))

         PASSED!

  54 TESTS PASSED!


Test Report
===========
test11-controlStructs.lisp                 54 TESTS PASSED!

Total test files: 1.
Total test cases: 54.
